# public / 정적 파일 폴더

여기에 올린 파일은 사이트 루트에서 바로 접근됩니다.
예) public/cursor-buddy.png → https://<사이트>/cursor-buddy.png

## 커서 버디 이미지
- 이 폴더에 `cursor-buddy.png` (또는 .svg) 이름으로 이미지를 올리면
  그 이미지가 마우스를 멀찌감치 따라옵니다.
- 정사각형 이미지 권장(원형 56px로 크롭되어 표시됨).
- 파일이 없으면 커서 버디는 표시되지 않습니다(안전).
